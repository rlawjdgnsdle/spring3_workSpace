<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gms.web.mapper.MemberMapper">
	<select id="selectOne" resultType="com.gms.web.domain.MemberDTO">
		SELECT * FROM MEMBER WHERE MEMID LIKE #{MEMID}
	</select>
	<select id="login" resultType="com.gms.web.domain.MemberDTO">
		SELECT MEMID, TEAMID, MEMNAME, AGE, ROLL, PASS, SSN, GENDER FROM MEMBER
		WHERE MEM_ID LIKE #{MEMID} AND PASS LIKE #{PASS}
	</select>
	<select id="add" resultType="com.gms.web.domain.MemberDTO">
		INSERT INTO MEMBER (MEMID, PASS, MEMNAME, SSN, TEAMID, ROLL, SUBJECT)
		VALUES(#{MEMID},#{PASS},#{MEMNAME},#{SSN},#{TEAMID},#{ROLL},#{SUBJECT})
	</select>
	<select id="update" resultType="com.gms.web.domain.MemberDTO">
		UPDATE MEMBER SET %s = ?, %s = ?, %s = ? WHERE MEM_ID LIKE #{MEMID}
	</select>
	<select id="delete" resultType="com.gms.web.domain.MemberDTO">
		DELETE FROM MEMBER WHERE MEM_ID LIKE #{MEMID} AND PASSWORD LIKE #{PASS}
	</select>

</mapper>
